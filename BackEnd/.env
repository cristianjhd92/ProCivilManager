# File: BackEnd/.env.example                                              # Ejemplo para copiar a .env
# Descripción: Variables de entorno para el servidor Node/Express + DB    # NO subir .env real al repositorio

# =========================
# App / Servidor HTTP
# =========================

# Puerto donde corre Express (en dev suele ser 5000).
PORT=5000

# Modo de ejecución (development|production|test).
NODE_ENV=development

# Origen permitido para CORS/Socket.io. Usa una sola URL.
# Si necesitas varios orígenes, contrólalos en código.
CORS_ORIGIN=http://localhost:3000


# =========================
# Base de datos (MongoDB)
# =========================

# Cadena de conexión a MongoDB/Atlas (reemplaza user/pass/host/db).
# Ejemplo:
# mongodb+srv://USUARIO:CONTRASEÑA@CLUSTER.mongodb.net/NOMBRE_DB?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://YOUR_USER:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/YOUR_DB?retryWrites=true&w=majority

# Logs de consultas Mongoose (true en dev, false en prod).
MONGOOSE_DEBUG=true


# =========================
# Autenticación (JWT)
# =========================

# Clave secreta para firmar JWT (usa una cadena larga y aleatoria).
JWT_SECRET=CHANGE_THIS_TO_A_LONG_RANDOM_SECRET


# =========================
# Correo saliente (SMTP)
# =========================

# Host SMTP. Para Gmail: smtp.gmail.com
SMTP_HOST=smtp.gmail.com

# Puerto SMTP. 465 = SSL (secure true). 587 = STARTTLS (secure false).
SMTP_PORT=465

# true para SSL directo (465). false para STARTTLS (587).
SMTP_SECURE=true

# Usuario SMTP (en Gmail suele ser el correo completo).
SMTP_USER=youraccount@gmail.com

# App Password SIN espacios (si Gmail te da "abcd efgh ijkl mnop", aquí va "abcdefghijklmnop").
SMTP_PASS=YOUR_APP_PASSWORD_NO_SPACES

# Remitente visible para los usuarios (debe ser válido/permitido por tu proveedor).
MAIL_FROM="Procivil Manager" <youraccount@gmail.com>

# Correo de soporte usado en plantillas / reply-to por defecto.
SUPPORT_EMAIL=youraccount@gmail.com


# =========================
# Frontend / URLs
# =========================

# URL del frontend (usada para enlaces como recuperación de contraseña).
FRONTEND_URL=http://localhost:3000


# =========================
# Notificaciones internas
# =========================

# Destinatarios internos para mensajes de contacto (separa con comas si son varios).
CONTACT_NOTIFICATIONS_TO=youraccount@gmail.com

# Destinatarios internos para solicitudes de proyecto (separa con comas si son varios).
PROJECT_NOTIFICATIONS_TO=youraccount@gmail.com