# File: BackEnd/.env.example
# Description: Variables de entorno de ejemplo del backend de ProCivil Manager (PCM).
#              Copia este archivo como `.env` y reemplaza los valores por los reales.
#              NUNCA subas el `.env` real a GitHub.

# --- Base de datos ---
# Cadena de conexión a MongoDB (Atlas o local)
MONGO_URI=mongodb+srv://USUARIO:CONTRASENA@HOST.mongodb.net/NOMBRE_BASE?retryWrites=true&w=majority

# Puerto en el que se levanta el backend
PORT=5000

# Clave secreta para firmar JWT (usa una cadena larga y aleatoria)
JWT_SECRET=CAMBIA_ESTA_CLAVE_POR_UNA_MUY_SEGURA

# Entorno de ejecución: development | production | test
NODE_ENV=development

# URL del frontend para configurar CORS y enlaces en correos
FRONTEND_URL=http://localhost:3000

# --- Correo base que enviará los mensajes ---
# Correo emisor principal (desde donde se enviarán los correos)
MAIL_USER=tu-correo-principal@dominio.com

# --- Credenciales OAuth2 de Gmail ---
# Estas credenciales se obtienen desde Google Cloud Console
GMAIL_CLIENT_ID=TU_CLIENT_ID_DE_GOOGLE
GMAIL_CLIENT_SECRET=TU_CLIENT_SECRET_DE_GOOGLE
GMAIL_REDIRECT_URI=http://localhost

# --- Refresh token ---
# Refresh token generado con las credenciales anteriores (NO lo compartas nunca)
GMAIL_REFRESH_TOKEN=TU_REFRESH_TOKEN_DE_GMAIL

# --- Destinatarios internos para notificaciones ---
# Lista separada por comas de correos internos que reciben notificaciones del sistema
CONTACT_RECIPIENTS=correo1@empresa.com,correo2@empresa.com

# --- URL de la imagen del logo de la aplicación ---
PCM_LOGO_URL=https://ruta-a-tu-logo.png
