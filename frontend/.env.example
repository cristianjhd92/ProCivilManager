# File: FrontEnd/.env.example
# Description: Variables de entorno de ejemplo para el frontend (Vite) de ProCivil Manager (PCM).
#              Copia este archivo como `.env` y reemplaza los valores por los reales.

# URL base del backend en entorno local o remoto
VITE_API_URL=http://localhost:5000/api

# URL del servidor de Socket.io (normalmente el mismo host del backend)
VITE_SOCKET_URL=http://localhost:5000

# Nombre de la app para títulos y UI
VITE_APP_NAME=ProCivil Manager

# Entorno: development | production | test
VITE_ENV=development
