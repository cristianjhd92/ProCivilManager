# File: FrontEnd/.env.example
# Description: Plantilla de variables de entorno para el frontend (Vite) de ProCivil Manager (PCM).
#              Sirve como ejemplo para configurar el archivo .env real.

VITE_API_URL=http://localhost:5000/api                  # URL de la API de backend
VITE_SOCKET_URL=http://localhost:5000                   # URL del servidor de Socket.io
VITE_APP_NAME=ProCivil Manager                          # Nombre de la app para títulos y UI
VITE_ENV=development                                    # Entorno: development | production | test
